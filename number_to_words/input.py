import convert_input

# Outputs the question to the terminal and takes in the user input as a raw_input.
# Prints the output.

raw_number = raw_input("What number would you like to convert to words? ")

print convert_input.convert_input_number(raw_number)
	